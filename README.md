# tsxpress - Typescript Express Generator

tsxpress is a tool to generate an opinionated Typescript-Express directory structure/setup to allow for faster development.

###### Usage: `npx tsxpress --name <PROJECT_NAME>`

